name: Build and Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Build environment'
        type: environment
        required: true
      branch:
        description: 'Branch to checkout'
        type: string
        default: master
        required: true

jobs:
  build:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    env:
      HUGO_ENVIRONMENT: ${{ github.event.inputs.environment }}
    steps:

      - name: Checkout
        uses: actions/checkout@v3
        with:
          repository: groupdocs-cloud/products.groupdocs.cloud
          ref: ${{ github.event.inputs.branch }}
          token: ${{ secrets.PAT }}

      - name: Setup Node.js
        uses: actions/setup-node@v3

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          extended: true

      - name: Install Node.js Dependencies
        run: npm install

      - name: Process markdown
        run: npm run generate

      - name: Build
        run: hugo --config=config-stage.toml

      - name: Set bucket for deployment
        uses: jacobtomlinson/gha-find-replace@v2
        with:
          find: "products-qa.groupdocs.cloud"
          replace: ${{ secrets.AWS_S3_BUCKET_NAME }}
          include: "config-stage.toml"

      - name: Set bucket
        run: hugo deploy --config=config-stage.toml

